import{initializeApp}from'https://www.gstatic.com/firebasejs/12.0.0/firebase-app.js';import{getDatabase,ref,onValue,runTransaction,set}from'https://www.gstatic.com/firebasejs/12.0.0/firebase-database.js';import{firebaseConfig}from'./firebase-config.js';const db=getDatabase(initializeApp(firebaseConfig));const room=()=>((new URLSearchParams(location.search).get('room')||'tort').replace(/[^a-zA-Z0-9_-]/g,'').slice(0,60)||'tort');const c=()=>ref(db,`counters/${room()}/count`);export const subscribe=(ok,err)=>onValue(c(),s=>ok(Math.max(0,Number(s.val())||0)),err);export const changeCount=d=>runTransaction(c(),v=>Math.max(0,(Number(v)||0)+d));export const resetCount=()=>set(c(),0);